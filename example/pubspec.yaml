name: gherkin_example
description: Example of Gherkin Wrapper usage
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  flutter_gherkin_generator:
    path: ../flutter_gherkin_generator
  flutter_gherkin_wrapper:
    path: ../flutter_gherkin_wrapper
  build_runner: ^2.4.6

flutter:
  uses-material-design: true
